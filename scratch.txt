in the current system in production, not mmx here is how it works. The equivalent of MMX is called FiduTrader, it 's a two tier application with no backend but only a UI and a database. when order is created in PAR with BNP via LOC with a status NEW. The Deposits application (backoffice) detects it via a quartz then route it to LOC database. When the order is executed in LOC. A quartz in the Deposits application detect it and create a contract in the Deposits database in LOC, the contract is enriched with the fees and taxes. When the back-officer in LOC validate the contract. Deposits application generates the booking transaction to the booking system named "Transactions 2" then update order status in FiduTrader database to executed then boradcast the executed contract to "Deposits" PAR