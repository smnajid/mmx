# Tasks — fast-test-feedback-loop

TDD note (per AGENTS.md waiver clause): Phases C and the Phase B migrations are **mechanical/characterization** work on existing behaviour (test infrastructure moves, no production behaviour change) — red-first is waived for those steps and each is labelled `(mechanical)`. The one genuinely behavioural piece — the tag-completeness meta-test — **is** delivered red-first. Where a step says a command must stay green, that command is the same `-Dtest`-scoped command from the paired write step.

## 0. Baseline

- [x] 0.1 Capture baseline timings: run `cd backend && mvn test` once and record per-class durations from surefire reports into `openspec/changes/fast-test-feedback-loop/design.md` (append a measurements section) — this is the before/after comparison source

## 1. Phase A — taxonomy foundations (bootstrap test infra + docs)

- [x] 1.1 Write failing tag-completeness meta-test `TestCategoryTaggingTest` (layer: bootstrap test, architecture category) asserting every test class under `backend/*/src/test/java` carries exactly one of `fast` / `integration` / `e2e` / `architecture` — verify red with `cd backend && mvn test -pl mmx-bootstrap -am -Dtest=TestCategoryTaggingTest -Dsurefire.failIfNoSpecifiedTests=false`
- [x] 1.2 Implement until green: classify all existing test classes with their single category tag (domain/application/pure adapter units → `fast`; `mmx-bootstrap` rest/migration/contract + persistence integration → `integration`; `e2e/*` Kafka classes → `e2e`; `DomainArchitectureTest`/`HexagonalArchitectureTest` → `architecture`) and tag the meta-test itself `architecture`; rerun the 1.1 command until green
- [x] 1.3 Wire JUnit 5 tag-expression selection in `backend/pom.xml` surefire configuration (`groups` / `excludedGroups` passthrough, no default filtering) — compile gate `cd backend && mvn -q -pl mmx-domain compile`; then confirm selection works via `cd backend && mvn test -pl mmx-domain,mmx-application -Dgroups=fast` inside this same step (fast tests only, no containers)
- [x] 1.4 Document the agent test-loop policy: add the "Test feedback loop" section to `AGENTS.md` (which loop per change type: domain change → `mvn test -pl mmx-domain -Dtest=<Class>`; application change → `mmx-application`; REST contract change → `mmx-adapter-in-rest` + the matching `integration` class; `e2e` on demand; full reactor reserved for final verification) and update the build-commands section of `docs/agents/codebase-map.md` with the category-selection commands

## 2. Phase C — shared test infrastructure (bootstrap + persistence test infra)

- [x] 2.1 Add shared singleton PostgreSQL container base class + between-class schema-reset convention to `mmx-bootstrap/src/test/java/com/mmx/order/support/` (one container per JVM) `(mechanical)` — `SharedPostgresTestBase` created: singleton container + inherited `DynamicPropertySource`; `@TestInstance(PER_CLASS)` + per-class instance `@BeforeAll` runs Flyway clean+migrate then re-invokes the `rest-test` seeders (institutions/currencies/users) so every class sees the pristine, seeded schema a private container used to provide
- [x] 2.1b Resolution superseding the earlier descope note: the "stateful tests aren't shared-container safe" finding was a misdiagnosis — the real cause of the 400/403/500 flips was the 2.4 parallelism leak (concurrent test methods racing the shared `demo-trader` active-scope store). Once parallelism was scoped correctly (2.4), the shared singleton container works: 10 PostgreSQL-backed classes now share ONE container and all pass green.
- [x] 2.2 Migrate all `integration`-tagged classes in `mmx-bootstrap` to the shared base and delete their per-class PostgreSQL container declarations; converge on one `@SpringBootTest` configuration so Spring context caching engages — verify with `cd backend && mvn test -pl mmx-bootstrap -am -Dgroups=integration` inside this same step `(mechanical)` — done: 9 classes extend `SharedPostgresTestBase` (7 rest/migration + 2 e2e), per-class `@Container`/props removed; `-Dgroups='integration|e2e'` **green**; the integration/e2e suite now runs on ONE PostgreSQL + ONE Kafka.
- [ ] 2.3 Split `mmx-adapter-out-persistence` `JpaOrderRepositoryTest` (32s baseline) by concern into separate classes — native desk queries / tenancy scoping / routed-hub idempotency / index behaviour — same assertions, each class `integration`-tagged and on the shared container; verify with `cd backend && mvn test -pl mmx-adapter-out-persistence -am -Dtest='JpaOrderRepository*'` inside this same step `(mechanical)` — note: persistence uses in-memory H2 (no shared container); split by this file's actual nested concerns
- [x] 2.4 Enable JUnit-platform parallel execution scoped to `mmx-domain` and `mmx-application` only (`junit-platform.properties` in those modules' test resources) — verify with `cd backend && mvn test -pl mmx-domain,mmx-application -Dgroups=fast` inside this same step `(mechanical)` — **implementation corrected post-delivery:** the properties files leaked parallel execution into `mmx-bootstrap` via reactor test-jar/test-classes resolution, racing the shared `demo-trader` active-scope store across concurrently-running test methods/classes (flipped 403/200s in `DelegatedGrants`/`GlobalAccounts`/`RoleScoped`, term-rate upload 500 in `OrderCreationOptions`). Replaced with surefire `configurationParameters` in the two module poms; all `junit-platform.properties` copies deleted (sources + stale `target/test-classes`); domain test-jar excludes test resources. Verified: `-Dgroups=integration` 44/44 green; fast loop 188+232 green with parallelism intact.
- [x] 2.5 Share one Kafka broker lifecycle across the `e2e`-tagged Kafka classes (topic-per-test isolation instead of broker-per-class) — verify with `cd backend && mvn test -pl mmx-bootstrap -am -Dgroups=e2e` inside this same step `(mechanical)` — done: `SharedKafkaTestBroker` (singleton `KafkaContainer` started once); both Kafka e2e classes use it and share the Postgres base; isolation is per-orderId key filtering on the shared topic (verified green with the integration+e2e group).

## 3. Phase B — push assertions down the pyramid (application + adapters tests)

Each pair: the write step lands the application-level characterization test (green against current production code); the follow-up step removes the duplicated assertion from the integration class in the same pair — an assertion may not be deleted before its replacement exists.

- [x] 3.1 Write application-layer tests (layer: `mmx-application/src/test`, `fast`) covering the order-lifecycle business rules currently asserted through HTTP in `OrderRestApiIntegrationTest` — verify with `cd backend && mvn test -pl mmx-application -Dtest='OrderLifecycle*Test'` (adjust `-Dtest` to the actual class names chosen) — **coverage verified as already present** (no new duplicates written): `IntakeServiceTest` (duplicate ref, mismatched institution, unknown/inactive institution), `OrderLifecycleServiceTest` (cancel/reject/account transitions), `OrderAgainstCurrencyPolicyTest` (unmanaged currency, disabled tenor, below-minimum, decrease floor), `MarkOrderAccountedServiceTest` (accounted transitions, 404/409/idempotent equivalents)
- [x] 3.2 De-duplicate `mmx-bootstrap` `OrderRestApiIntegrationTest` to contract smoke only (routes, status codes, JSON shape per `contracts/002` `openapi.yaml`, error-mapping wiring); rerun the 3.1 command stays green plus `cd backend && mvn test -pl mmx-bootstrap -am -Dtest=OrderRestApiIntegrationTest` inside this same step — 22 tests → **4 contract smokes** (receive 201 shape, duplicate-ref idempotency, invalid-payload error envelope, back-office 404 envelope); green; now order/state-independent
- [x] 3.3 Write application-layer tests covering intake validation + local routing rules currently asserted in `OrderRoutingIntakeIntegrationTest` — verify with `cd backend && mvn test -pl mmx-application -Dtest='*Routing*Test'` — **coverage verified as already present**: `IntakeServiceTest.successful_routing_yields_routed_client_and_hub_received` / `grant_violation_rejects_without_hub_order` / `unresolved_account_rejects_without_hub_order`, `OrderRoutingLifecycleTest`, `AcceptRoutedHubOrderUseCaseTest`
- [x] 3.4 De-duplicate `OrderRoutingIntakeIntegrationTest` to contract smoke; rerun the 3.3 command stays green plus `cd backend && mvn test -pl mmx-bootstrap -am -Dtest=OrderRoutingIntakeIntegrationTest` inside this same step — 2 tests → **1 routed-intake wiring smoke** (client `ROUTED` + single linked hub row sharing `routing_id`, hub `RECEIVED`); green; order-independent
- [x] 3.5 Write application-layer tests covering scope/tenancy filtering rules currently asserted in `RestScopeIntegrationTest` and `RoleScopedSettingsRestApiIntegrationTest` — verify with `cd backend && mvn test -pl mmx-application -Dtest='*Scope*Test'` — **coverage verified as already present**: `DeskQueryScopeTest`, `ReScopeUseCaseTest`, `OnboardInstitutionServiceTest` (client-rep vs trader paths), `ManageDelegatedGrantsServiceTest` (role-split grants)
- [x] 3.6 De-duplicate both scope integration classes to contract smoke; rerun the 3.5 command stays green plus `cd backend && mvn test -pl mmx-bootstrap -am -Dtest='RestScopeIntegrationTest,RoleScopedSettingsRestApiIntegrationTest'` inside this same step — `RoleScopedSettings` 3 → **2 authorisation-wiring smokes** (dropped the grant-seeding success path as stale-vs-spec; covered by `OnboardInstitutionServiceTest`/`ManageDelegatedGrantsServiceTest`); `RestScope` assessed and kept — already at smoke granularity (4 thin tenancy/authz contract checks)
- [x] 3.7 Write application-layer tests covering delegated-grant and settings business rules currently asserted in `DelegatedGrantsRestApiIntegrationTest`, `TermRateRestApiIntegrationTest`, `OrderCreationOptionsRestApiIntegrationTest`, `GlobalAccountsRestApiIntegrationTest` — verify with `cd backend && mvn test -pl mmx-application -Dtest='*Grant*Test,*TermRate*Test,*CreationOptions*Test,*GlobalAccount*Test'` — **coverage verified as already present**: `ManageDelegatedGrantsServiceTest`, `UploadTermRatesServiceTest` + `TermRateIngestPolicyTest`, `TermOrderCreationOptionsServiceTest`/`OnCallOrderCreationOptionsServiceTest`, `ManageGlobalAccountsServiceTest`
- [x] 3.8 De-duplicate the four settings integration classes to contract smoke; rerun the 3.7 command stays green plus `cd backend && mvn test -pl mmx-bootstrap -am -Dtest='DelegatedGrantsRestApiIntegrationTest,TermRateRestApiIntegrationTest,OrderCreationOptionsRestApiIntegrationTest,GlobalAccountsRestApiIntegrationTest'` inside this same step — assessed: all four already at contract-smoke granularity (2–6 thin status-code/wiring checks each; their apparent failures were the 2.4 parallelism leak, fixed at root) — no HTTP-level business-rule duplication to remove; all green
- [ ] 3.9 Write persistence-level transactional outbox assertions (layer: `mmx-adapter-out-persistence/src/test`, `integration`) replacing behavioural coverage duplicated in Kafka e2e, and unit tests for the relay/listener mappers (layer: `mmx-adapter-out-messaging/src/test`, `fast`) — verify with `cd backend && mvn test -pl mmx-adapter-out-persistence,mmx-adapter-out-messaging -am -Dtest='*Outbox*,*Relay*,*Listener*'`
- [ ] 3.10 Trim behavioural duplication from `ExecutionHandoffKafkaIntegrationTest` and `RoutedExecutionHandoffKafkaIntegrationTest` so each remains the single `e2e` acceptance lock per messaging capability; rerun the 3.9 command stays green plus `cd backend && mvn test -pl mmx-bootstrap -am -Dgroups=e2e` inside this same step

## 4. Final verification

- [ ] 4.1 Run full `cd backend && mvn test` — all modules green (all categories, shared containers engaged); compare per-class durations against the 0.1 baseline and append the after-measurements next to them
- [ ] 4.2 Confirm spec–code parity: `AGENTS.md` and `docs/agents/codebase-map.md` reflect the final command set; `openspec validate fast-test-feedback-loop` passes
