import { Routes } from '@angular/router';
import { AssignedOrderListComponent } from './assigned-order-list.component';

export const ASSIGNED_ORDERS_ROUTES: Routes = [
  { path: '', component: AssignedOrderListComponent },
];
