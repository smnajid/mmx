import { Routes } from '@angular/router';
import { TermOrderListComponent } from './term-order-list.component';

export const TERM_ORDERS_ROUTES: Routes = [
  { path: '', component: TermOrderListComponent },
];
