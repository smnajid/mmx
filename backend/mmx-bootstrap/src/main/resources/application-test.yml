spring:
  datasource:
    # Overridden by Testcontainers in integration tests via @DynamicPropertySource
    url: jdbc:tc:postgresql:16-alpine:///mmx
    username: mmx
    password: mmx
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true

logging:
  level:
    com.mmx: DEBUG
