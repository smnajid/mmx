<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.5</version>
        <relativePath/>
    </parent>

    <groupId>com.mmx</groupId>
    <artifactId>mmx-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>mmx-backend</name>
    <description>Money Market Order Processing backend parent project</description>

    <modules>
        <module>mmx-domain</module>
        <module>mmx-application</module>
        <module>mmx-adapter-in-rest</module>
        <module>mmx-adapter-out-persistence</module>
        <module>mmx-adapter-out-integration</module>
        <module>mmx-bootstrap</module>
    </modules>

    <properties>
        <java.version>25</java.version>
    </properties>

    <!--
        All test dependency versions (JUnit 5, AssertJ, Mockito, Testcontainers, etc.)
        are managed by Spring Boot 4.0.5's BOM inherited from spring-boot-starter-parent.
        Child modules may declare them without a <version> tag.
    -->

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
